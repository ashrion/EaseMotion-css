# Grid Gap

## What does this do?
Grid Gap component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="grg-primary">Grid Gap Primary</div>
```

## Why is it useful?
Reusable grid gap UI element. Dark theme with CSS custom properties.
